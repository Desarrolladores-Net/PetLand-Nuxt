export interface CreateInterfaceRM{
    formId: String,
    message: String,
    typeQuestion: Number
}

export interface QuestionItem{
    formId: String,
    id: String,
    message: String,
    typeQuestion: Number
}